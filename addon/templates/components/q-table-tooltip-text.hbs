{{#tooltip-wrap styleClass=additionalParams.styleClass  as |yield|}}
    {{#if yield.isHoverArea}}
        {{displayText}}
    {{else if yield.isTooltipArea}}
        {{tooltipText}}
     {{/if}}
{{/tooltip-wrap}}